version: '3'
services:
  frontend:
    build: ./frontend
    container_name: frontend
    depends_on:
      - backend
  backend:
    build: ./backend
    command: ./docker-entrypoint.sh
    container_name: backend
    env_file:
      - ./.env

  resume:
    build: ./my_cv
    container_name: resume

networks:
  default:
    external:
      name: docker_host
